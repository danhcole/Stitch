int main()
{
	int a[5][5];
	int b[5][5];
	int c[5][5];

	int d = 1;
	int i = 0;
	int j = 0;
	int k = 0;

	for (i=0; i<5; i= i + 1)
	{
		for (j=0; j<5; j = j + 1)
		{
			a[i][j] = d;
			d = d + 1;
			b[i][j] = d;
			d = d + 1;
		}
	}

	for (i=0; i<5; i= i + 1)
	{
		for (j=0; j<5; j = j + 1)
		{
			for (k=0; k<5; k = k + 1)
			{
				c[i][j] = c[i][j] + a[i][k] * b[k][j];		
			}

		}
	}

	print("finished");

	return 0;
}